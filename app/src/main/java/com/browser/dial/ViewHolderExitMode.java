package com.browser.dial;

import android.view.View;

import com.browser.ui.home.BaseViewHolder;

public class ViewHolderExitMode extends BaseViewHolder {
    public ViewHolderExitMode(View view) {
        super(view);
    }
}